# Panel de administración web

Pendiente — se construye en la siguiente fase del proyecto (React).

Va a consumir la API descrita en [`../backend/README.md`](../backend/README.md):
login, listado de pantallas con estado en vivo, emparejamiento, subida de contenidos,
armado de playlists, asignación y programación de horarios, y vista previa en vivo
(vía Socket.io, sala `admin`).
