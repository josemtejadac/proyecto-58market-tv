# Copia este archivo a ".env" (en la misma carpeta que docker-compose.yml) y pon
# tus propios valores. El archivo ".env" NUNCA se sube a GitHub (ver .gitignore).

POSTGRES_PASSWORD=cambia-esta-clave-de-base-de-datos
JWT_SECRET=cambia-esto-por-un-secreto-largo-y-aleatorio
ADMIN_USER=admin
ADMIN_PASSWORD=cambia-esta-clave
